# 無料食事イベント台帳 現在の仕様

この資料は、画面・終了処理・データ構造などの詳細仕様をまとめる。
通常のイベント調査では毎回読まない。UIや保存処理を変更するときだけ必要な部分を読む。

## 1. データの正本と基本構造

GitHubリポジトリ：`rensei11/01-event`

- `イベント一覧.json`：懇親会系年間台帳
- `展示会一覧.json`：展示会・大型企業カンファレンス年間台帳
- `実地レポート.json`：実地レポートの読み込み先一覧
- `実地レポート/`：実地レポート本文（月別JSON）
- `未参加イベント.json`：候補だったが実際には参加しなかった過去イベント
- `index.html`：本番表示
- `確認用.html`：現在のUI再設計・検品対象
- `終了処理保存Worker.js`：GitHubへ確定保存するCloudflare Workerソース
- `site/chunk0.txt` ～ `site/chunk5.txt`：移行前HTMLの復旧用

### 正本ルール
- 古い会話や古いバックアップより、GitHub上の現在ファイルを優先する。
- `visitDates` は実際に行った日。
- `scheduledDate` / `scheduledEndDate` は開催期間。
- 実地レポート本文を年間台帳へ重複保存しない。対応は `sourceReportId` で持つ。
- 過去結果はlocalStorageではなく年間台帳の `attendanceStatus` / `visitDates` を正本とする。

## 2. 本番と確認版

現在のUI開発対象は `確認用.html`。
ユーザーが本番反映を明示するまでは `index.html` を変更しない。
確認版の右上には、本番版と同様に「読込完了：懇親会○件 / 展示会○件 / レポート○件」を表示する。表示は各JSONと実地レポートファイルの取得成功を確認した後だけ成功色で出し、取得失敗時は失敗表示にする。

確認版URL：
`https://rensei11.github.io/01-event/%E7%A2%BA%E8%AA%8D%E7%94%A8.html`

## 3. 終了処理

- 終了日を過ぎ、参加結果が未確定のイベントを終了処理待ちにする。
- `attendanceStatus: attended` でも実地レポート未作成なら終了処理待ちに残す。
- 終了処理では、参加 / 未参加、実際に行った日、実地評価、実地レポートだけでなく、イベント名、会場、URL、参加条件、名前、事前評価、時間、内容・根拠も編集できる。
- 終了処理入力はブラウザ下書きへ自動保存するが、年間台帳の正本とは混ぜない。
- 自由編集対応後はWorkerの `/save-v2` を使う。
- 保存後はページを再読込し、GitHubを正本として再表示する。
- 終了処理は正式GitHubデータだけを基準にし、自由編集下書きの日付・区分等を混ぜない。
- 自由編集と終了処理は完全に別機能。

### Cloudflare Worker
- ソース：`終了処理保存Worker.js`
- Secret：`GITHUB_TOKEN`、`EDIT_KEY`
- 秘密値はGitHubへ保存しない。
- GitHub側の画面は `/save-v2` を使う。
- Cloudflare側へ最新Workerを反映するまでは、終了処理の「GitHubへ保存」を本番利用しない。
- データ・画面・Workerの修正が全部固まるまでは、ユーザーへ再貼り替えを何度も要求しない。最後に1回だけ行う。

## 4. 終了済みイベント

- 参加済み：年間台帳に `attendanceStatus: attended` と `visitDates`
- 未参加：`attendanceStatus: not_attended` と `visitDates: []`
- 終了済みで結果確定済みの行には「行く」「×」を出さず、`✓ 参加` または `× 未参加` を表示する。
- 複数日イベントは `visitDates` に含まれる日だけを実際の参加日とする。
- 開催期間全日を自動で参加扱いにしない。

## 5. 来場予定・除外

### 来場予定
- 青いチェック。
- チェックした行は薄い水色。
- カレンダーでは選んだ日だけ青丸。
- 複数日イベントの来場予定は日別管理する。
- 過去のlocalStorage予定が残っていても、今日より前の日は「来場予定」として表示しない。

### 除外
- 会場名左に空の四角。
- 選択すると赤背景＋白い「×」。
- 行は薄い灰色。
- 来場予定チェックとは同時選択不可。

参加予定・除外状態はイベントID単位でlocalStorageへ保存する。
既存イベントIDは変更しない。

## 6. 一覧表示

現在の確認版の上位表示：
- すべて
- 来場予定
- 参加済み

通常一覧は1イベント1行。
複数日イベントも通常一覧では1件のまま。

参加済み表示では実際の `visitDates` ごとに日記形式で表示する。
複数回来場した場合は来場日ごとに行を出し、実地レポート本文は最初の来場日にだけ表示する。

`attendanceStatus: attended` だが `visitDates` が空のイベントも消さず、「参加済み・実際に行った日が未入力」として残す。

### 表示
- 日付は `9/16(水)` のような短い形式。
- 事前評価 `A+。理由...` は評価を太字1行目、理由を2行目。
- 実地表示は `実地レポート　評価：○`。
- 終了済みで未確定・来場日不足・レポート不足なら「終了処理」。
- 確定済みなら「記録修正」。

## 7. 自由編集

- 自由編集はブラウザの構造化下書き `free_food_event_ledger_2026_structured_edit_v2` に保存する。
- 旧方式のtbody HTML丸ごと保存、古いEDIT_DRAFT_KEYからのイベント全体復元は使わない。
- 最新GitHubデータを古いブラウザデータで上書きしない。
- 編集開始時は「すべて」表示へ戻る。
- イベント名、会場、名前、参加条件、事前評価、時間、内容は表上で直接編集。
- 日付は日付入力欄、区分・URLは選択行のボタンから変更。
- 新規追加・削除も下書きとして保持する。
- 並び順は手動上下移動ではなく自動。

並び順：
1. 日付
2. 展示会 / 懇親会
3. 会場

東京ビッグサイトは東 → 西 → 南、ホール番号順。

## 8. カレンダー

- 懇親会と展示会を合算する。
- 初期表示は現在月＋次2か月の3か月。
- 左右矢印で1か月ずつ移動。
- 表の年・月ボタンで、表の表示開始月とカレンダー先頭月を同じ月へ移動する。
- 月ボタン操作そのものではページ位置を勝手に移動しない。
- カレンダーの日付クリックは該当行へ移動。
- 月見出しクリックはその月の一覧へ移動。
- 確認版の月カレンダーは常に6週（42日マス）を表示し、5週で終わる月でも高さを変えない。
- 前月・翌月の日付は薄く表示し、その日付にイベント・来場予定・参加済みマークがある場合も当月より大幅に薄くする。
- イベント日は灰色丸。
- 来場予定日は青丸。
- 今日には赤い輪郭。

## 9. 実地レポート

- 専用画面へ分離せず、参加済みイベントと同じ表示内で扱う。
- 本文は `実地レポート/` の月別JSONを正本とする。
- 年間台帳へ本文を重複保存しない。
- `sourceReportId` で対応付ける。
- Excel移行時の `rating` と実地レポートが重複する場合は画面で二重表示しない。

## 10. 展示会・複数日イベント

- 展示会・大型企業カンファレンスは `展示会一覧.json`。
- 複数日開催は内部では1イベントのまま `date` と `endDate` で期間を持つ。
- Dayごとにプログラム自体が別物の場合だけ別イベントとしてよい。
- 実際に参加した日は `visitDates` に実来訪日だけ入れる。

## 11. Excel移行ルール

対象元：`2026年 企業展示会簡易レポ.xlsx`

- 「スケジュール」の薄い水色は「展示会以外」の強い手掛かりだが、色だけでは確定しない。
- 「超アミュジョブサミット」は対象外として再候補にしない。
- Microsoft AI Tour / AWS Summit / Google Cloud Next は大型イベントとして展示会側。
- 展示会内の交流会に固有名がある場合、展示会本体を懇親会へ重複登録せず、交流会を固有名で登録する。
- Excelの名前欄は内部では `名前コード：○○` として保持する。
- 公開画面では「名前コード：」は出さず、名前だけ表示する。
- 名前コードが赤字なら「審査ハードル高め（過去に申請拒否あり）」を付ける。
- 名前コード以外の文字色には原則意味を持たせない。
- 過去イベントは「参加」「未参加」を必ず区別する。
- 既存GitHub側により詳しい公式URL・評価・時間等がある場合は既存情報を優先する。
- 公開リポジトリなので、秘密情報・個別コード・公開に不適切な操作手順は保存しない。

## 12. 既知の整合性修正

2026-09-19までに以下を確認済み。
- GROWTH 祭 TOKYO 2026：`visitDates: ["2026-09-09"]`
- 国際物流総合展 2026：`visitDates: ["2026-09-09", "2026-09-10"]`
- FOODEX JAPAN 2026：3/10・3/11・3/13
- SMART ENERGY WEEK【春】2026：3/17・3/19
- イプロスAI 2026 夏：7/29・7/31
- 60th FISMA TOKYO：2026年11月12日～13日
- WOOD コレクション (モクコレ) 2026Plus：東京ビッグサイト 西1・2ホール、アトリウム

これらを古い資料から誤って戻さない。

## 13. 触らないもの

- `site/chunk*.txt`：復旧用
- 秘密情報、APIキー、ログイン情報
- 理由なく `index.html` 全体
- 古いブラウザ下書きからの全データ復元

細かな変更は必要最小限のファイルだけ修正する。
