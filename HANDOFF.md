# 無料食事イベント調査 引き継ぎ

## 目的
東京都内中心で、無料または具体的な無料参加経路があり、17:00以降にNetworking／Reception／After Party／懇親会などで食事・アルコール提供が期待できるイベントを探す。

## 重視条件
- 原則無料。招待コード・スポンサー招待など具体的な無料経路があれば可。
- 17:00以降の懇親・食事を最重視。ランチは重視しない。
- 150人以上を優先。100人未満は大幅減点。
- 軽食のみ、混雑過多、杯数課金は低評価。
- ホテル宴会場、普段入れない会場、料理の量・補充・質を重視。
- ユーザーはビールを飲まず、ワインも基本苦手。ブラックニッカクリアは低評価。
- 食品展示会は除外。
- 評価はA+～C程度。

## 正本
現在のイベント一覧・評価・会場・リンクはGitHub Pagesの台帳を正本とする。

- Repository: `rensei11/chatgpt-test`
- 公開URL: `https://rensei11.github.io/chatgpt-test/`

引き継ぎ文章にイベント一覧を重複して保存しない。次チャットでは必ずGitHub上の現物を読んで続ける。

## 現在の構成
- `index.html`
- `site/chunk0.txt` ～ `site/chunk5.txt`
- `HANDOFF.md`（このファイル）

`index.html` がchunkを読み込んで結合表示する。

## 通常ChatGPTからGitHub更新
通常チャットのGitHubコネクタで、実際に以下を確認済み。
- 新規ファイル作成
- 既存ファイル更新
- GitHub Pages反映

今後は原則、Codex／Workへ移行せず、通常チャットのGitHub接続で更新する。
ユーザー側は固定URLを再読み込みするだけ。Pages反映は実測で約15秒程度かかることがある。

## UI仕様
イベント名左：参加予定チェック。
- 通常の青いチェック。
- チェック時、そのイベント行の背景を薄い水色。
- カレンダー上ではその日を青丸。

会場名左：除外チェック。
- 通常は空の四角。
- クリックすると赤背景＋白い×。
- 説明文字は表示しない。
- 除外行は薄い灰色表示。
- 参加予定チェックとは同時選択不可。

カレンダー：
- PC日時基準で当月＋次2か月。
- イベント日は通常灰色丸。
- 参加予定イベントがある日は青丸。
- 今日には黒い輪郭。
- 日付クリックでその日のイベントへスクロール。

## 保存仕様
参加予定・除外状態はブラウザのlocalStorageに保存。
イベント固有IDを利用し、HTML更新後も状態をできるだけ維持する。
古い表全体が最新版を上書きしないよう、HTML版ごとの保存キーを使用。

## 成功済み
- GitHubコネクタ接続。
- 通常ChatGPTから書き込み成功。
- GitHub Pages固定URL化成功。
- UI変更をGitHubへ直接反映成功。
- チェック状態、行色、カレンダー色変更も反映確認済み。

## 失敗済み・再提案しない
ローカル `E:\無料食事イベント台帳.html` の自動更新は複数案を試して失敗。
- PowerShell常時監視：常駐をユーザーが希望せず却下。
- CMDでコピー：追加クリックが必要で却下。
- Chrome拡張＋Native Messaging：ChatGPTのダウンロード経路を捕捉できず失敗。

「監視なし・追加クリックなしでE:へ自動上書き」は再提案しない。現在はGitHub Pages方式を採用。

## 次チャット開始時
最初に以下を読む。
1. `HANDOFF.md`
2. 現在の `index.html` と必要な `site/chunk*.txt`

その上で、必要なWeb調査 → 台帳更新 → GitHub Pages再配信確認まで行う。

記憶や過去文章だけで台帳を再構築しない。現在のGitHub上のファイルを正本とする。
