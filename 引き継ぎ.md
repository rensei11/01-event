# 無料食事イベント調査 引き継ぎ

## この案件の目的
東京都内中心で、無料または具体的な無料参加経路があり、17:00以降に懇親会・Networking・Reception・After Party等で食事やアルコール提供が期待できるイベントを探す。

重視条件：
- 原則無料。招待コード等で無料になるものは可。
- 夕方以降の食事・懇親会を最重視。
- 150人以上を優先。小規模は大幅減点。
- 大人数でも軽食のみ・混雑過多は評価を下げる。
- ホテル宴会場、普段入れない会場、料理の量・補充・質を重視。
- ユーザーはビールを飲まない。ワインも基本苦手。ブラックニッカクリアは低評価。
- 杯数課金・チケット制は不可。
- 食品展示会は除外。

## 現在の正本
GitHubリポジトリ：`rensei11/chatgpt-test`

公開URL：
`https://rensei11.github.io/chatgpt-test/`

今後の候補一覧は、会話履歴ではなくGitHub上の現在の台帳を正本とする。

## 更新方法
通常ChatGPTからGitHubコネクタを使って、実際に新規作成・既存更新・GitHub Pages反映まで成功確認済み。

ユーザーの通常運用：
1. ChatGPTに「台帳を更新して」と依頼
2. ChatGPTがGitHubを直接更新
3. 同じ公開URLを再読み込みして確認

公開反映には実測で約15秒かかることがある。

Codex／Workへ移行せず、今回と同じ通常ChatGPTのGitHub接続を優先する。

## 現在のGitHub構成
- `index.html`：公開ページの入口
- `site/chunk0.txt` ～ `site/chunk5.txt`：台帳本体
- `引き継ぎ.md`：この案件の説明書

今後編集するときは、記憶だけで作り直さず、必ずGitHub上の現在のファイルを読んでから修正する。

## 現在のUI仕様
イベント名の左に参加予定用チェック、会場名の左に除外用チェックを置く。

参加予定チェック：
- 青いチェックマーク。
- チェック時、そのイベント行全体を薄い水色にする。
- カレンダー上でもその日を青丸にする。

除外チェック：
- 通常は空の四角。
- クリックすると赤背景＋白い「×」。
- 説明文字は表示しない。
- 除外した行は薄い灰色表示。
- 参加予定チェックとは同時選択不可。

カレンダー：
- 当月＋次2か月。
- イベントのある日は通常「灰色」の丸。
- 参加予定イベントがある日は「青色」の丸。
- 今日には黒い輪郭。
- 日付クリックでその日のイベントへスクロール。

参加予定・除外状態はlocalStorageに保存する。

## 今回成功したこと
- 通常ChatGPTからGitHubへ直接ファイル作成・更新できた。
- GitHub Pagesで固定URL公開できた。
- 台帳のUI修正をGitHub更新だけで公開反映できた。
- 参加予定行の水色化、除外チェックの赤×化、カレンダーの通常灰色／参加予定青色化まで反映確認済み。

## 失敗済みなので再提案しないこと
ローカル `E:\無料食事イベント台帳.html` の自動更新は複数方式で失敗。
- PowerShell常時監視：常駐監視が不要なので却下。
- ダウンロード後CMDコピー：追加クリックが必要なので却下。
- Chrome拡張＋Native Messaging：ChatGPTのダウンロード経路を取得できず失敗。

したがって、通常ChatGPTからEドライブへ直接自動上書きする案は再提案しない。

## ファイル名ルール
人が読むファイル名は原則日本語にする。
例：`引き継ぎ.md`、`現在の仕様.md`、`イベント一覧.json`。

`index.html` のようにシステム上意味のある名前だけ英語のまま残す。

## 次のチャットで最初にやること
ユーザーが「GitHubの引き継ぎ.mdと現在の台帳を読んで続けて」と言ったら、
1. `引き継ぎ.md` を読む
2. 現在の台帳ファイルを読む
3. その内容を基準に作業を続ける

イベント一覧を会話履歴から推測して復元しない。
