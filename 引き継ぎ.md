# 無料食事イベント調査 引き継ぎ

## 1. この案件は何をするものか

東京都内中心で、無料または具体的な無料参加経路があり、17:00以降に懇親会・Networking・Reception・After Party等で食事やアルコール提供が期待できるイベントを探し、台帳へ追加・更新する案件。

---

## 2. 最新情報はどこにあるか

この案件では、**GitHubに現在保存されているファイルを最新情報として扱う。**

GitHubリポジトリ：
`rensei11/01-event`

公開ページ：
`https://rensei11.github.io/01-event/`

イベントの最新データ：
`イベント一覧.json`（懇親会系）
`展示会一覧.json`（展示会・大型カンファレンス系）
`実地レポート.json`＋`実地レポート/`（参加実績）
`未参加イベント.json`（候補だったが実際には行っていない過去イベント）

作業するときは、過去のChatGPTの会話だけを信用せず、必ずGitHub上の現在のファイルを確認する。

古い会話・古いバックアップとGitHubの内容が違う場合は、**GitHubに現在ある内容を優先する。**

---

## 3. この案件で大事なルール

### イベント選定条件

- 原則無料。招待コード等で無料になるものは可。
- 夕方以降の食事・懇親会を最重視する。
- 150人以上を優先。小規模は大幅減点。
- 大人数でも軽食のみ・混雑過多は評価を下げる。
- ホテル宴会場、普段入れない会場、料理の量・補充・質を重視する。
- ユーザーはビールを飲まない。ワインも基本苦手。ブラックニッカクリアは低評価。
- 杯数課金・チケット制は不可。
- 食品展示会は除外。
- 食事条件が弱くても、価値の高い来場特典（ギフトカタログ等）があるイベントは参考候補として台帳に残してよい。

### 調査方法

- 新規探索は原則1週間単位で区切る。
- 直近約3週間は情報追加・申込開始が起こりやすいため、同じ週を3～4日程度の短い間隔でも再確認する。
- 1か月分を一括検索して「該当なし」と結論づけない。実際に確認した範囲だけ報告する。
- 公式ページだけでなく、主催企業のイベント一覧、セミナー一覧、タイムテーブル、来場特典ページも確認する。
- 「懇親会」「Networking」「Reception」「After Party」に加え、「来場特典」「ギフト」「交流会」も検索語に含める。
- 不明な情報は推測で補わない。

---

## 4. ファイルの役割

- `README.md`
  - 人間向けの簡単な案内。

- `引き継ぎ.md`
  - ChatGPT等のAI向け説明書。
  - 今読んでいるこのファイル。

- `イベント一覧.json`
  - 懇親会系の2026年年間台帳。今後の予定だけでなく、終了済みの参加・未参加履歴も残す。
  - イベント名、日付、会場、参加条件、評価、夜の内容、公式URL等を保存する。
  - Excelの「名前コード」がある場合は `conditionLines` の先頭に保持する。

- `実地レポート.json`
  - 実地レポートの読み込み先一覧（manifest）。詳細本文は `実地レポート/2026-01-03.json` ～ `実地レポート/2026-09.json` に分割保存する。
  - 実際に参加したイベントだけを実地レポートとして表示する。
  - `visitDates` は実際に行った日、`scheduledDate` / `scheduledEndDate` は開催期間。混同しない。

- `未参加イベント.json`
  - スケジュールには存在したが実際には行っていない過去イベントを保存する補助履歴。
  - `attendanceStatus` は `not_attended`、`visitDates` は空配列。
  - 「レポートがないだけ」と「行っていない」を区別するための補助データ。イベント一覧側にも `attendanceStatus: not_attended` と結果を残す。

- `展示会一覧.json`
  - 展示会および Microsoft AI Tour / AWS Summit / Google Cloud Next のような大型企業カンファレンスの2026年年間台帳。終了後も削除しない。
  - 複数日開催は1イベントのまま `date` と `endDate` で期間を持つ。Day1/Day2へ機械的に分割しない。
  - Dayごとにプログラム自体が別物の場合だけ別イベントとして扱ってよい。
  - 展示会の予定チェックは懇親会と同じlocalStorageに保存し、共通カレンダーへ反映する。

- `index.html`
  - 表示画面、3か月カレンダー、参加予定チェック、除外チェック等のプログラム。
  - 表示方法や機能を変更するときだけ更新する。
  - イベント追加だけのために書き換えない。

- `site/chunk0.txt` ～ `site/chunk5.txt`
  - 移行前のHTML本体。
  - 現在の公開ページでは使用していない。
  - 復旧用として当面残す。通常更新では触らない。

### 現在のUI仕様

終了済みイベントの結果：
- 参加済みイベントはイベント一覧側にも `attendanceStatus: attended` と `visitDates` を保持する。
- 行っていないイベントはイベント一覧側にも `attendanceStatus: not_attended` と `visitDates: []` を保持する。
- 複数日イベントでは日ごとの表示行ごとに、`visitDates` に含まれる日は「参加」、含まれない日は「未参加」と表示する。
- 終了済みで結果確定済みの行には編集可能な「行く」「×」チェックを出さず、年間台帳の `attendanceStatus` / `visitDates` から `✓ 参加` または `× 未参加` を表示する。過去結果はlocalStorageでは管理しない。
- 実地レポート本文はイベント一覧へ重複保存せず、`sourceReportId` で実地レポート側と対応付ける。

参加予定チェック：
- 単日イベントはイベント名の左に従来どおり「行く」チェックを置く。
- 内部データでは複数日開催を1イベントのまま保持するが、画面では開催日ごとに別行へ展開する。
- 3日開催ならイベント名を `イベント名（1）`、`イベント名（2）`、`イベント名（3）` と表示する。
- 各日の行にそれぞれ「行く」チェックと「×」を表示する。「行く」はその日のみを来場予定にする。
- 複数日イベントでは、会場名の下にも従来の `行く日 □16(水) □17(木) ...` を残す。
- 会場下の「行く日」と各日行の「行く」は同じ `visitDates` を操作し、完全に同期する。どちらで17日を選んでも17日の行だけがチェック・水色になり、17日だけカレンダーが青丸になる。
- 選んだ日だけカレンダーを青丸にし、来場予定画面にも選んだ日の行だけ表示する。
- 青いチェックマーク。
- チェック時、そのイベント行全体を薄い水色にする。
- カレンダー上でもその日を青丸にする。

- イベント一覧・来場予定の日付表示は `9/16(水)` のようなスラッシュ表記に統一し、「9月16日(水)」のような長い表記を使わない。

除外チェック：
- 会場名の左に置く。
- 通常は空の四角。
- クリックすると赤背景＋白い「×」。
- 説明文字は表示しない。
- 除外した行は薄い灰色表示。
- 参加予定チェックとは同時選択不可。

画面切替：
- 上部左側に「イベント一覧」「来場予定」「実地レポート」の3ボタンを置く。
- 「イベント一覧」と「来場予定」では、共通カレンダーの下に「展示会」「懇親会」「両方」の順で、目立つ大きめの子タブを置く。
- 「来場予定」は「行く」にチェックしたイベントだけを表示する。
- 上部の「来場予定」をクリックして開くときは、子タブを必ず「両方」から開始する。
- 最後に開いていた上位タブ（イベント一覧／来場予定／実地レポート）と子タブ（展示会／懇親会／両方）はlocalStorageに保存し、ページ再読込時は同じ表示状態から開く。
- 来場予定の「両方」表示では、同じ日は展示会を先、懇親会を後にする。懇親会が複数ある場合は、夜の内容等に記載された開始時刻が早いものを先にする。
- 「両方」表示では種類タグを表示し、展示会は濃い緑、懇親会は濃い橙色系＋白文字にして、参加予定の薄い水色背景でも判別しやすくする。
- 「チェックを初期化」は誤操作防止のため右側に離して配置する。
- 初期化時は必ず確認ダイアログを出し、一発では消さない。

ページ操作：
- ページを一定量スクロールすると、右下に丸い `↑` ボタンを表示する。
- `↑` を押すとページ最上部へ滑らかに戻る。

イベント一覧・来場予定の共通カレンダー：
- 懇親会と展示会の両方を合算する。
- 初期表示は現在月＋次2か月の3か月。
- 左右の矢印ボタンで表示開始月を1か月ずつ前後できる。
- カレンダー上部に年プルダウンを置く。現在は2026年データのみだが、2027年等のデータが増えたら候補年を自動追加する。
- 年プルダウンの右に1月～12月の月ボタンを置く。押した月を先頭に3か月表示し、一覧もその月の最初のイベントへスクロールする。
- 各カレンダーの「2026年9月」のような月名をクリックしても、その月の一覧先頭へスクロールする。
- 懇親会または展示会がある日は通常「灰色」の丸。
- どちらかに「行く」チェックがある日は「青色」の丸。
- 懇親会と展示会の両方に予定があっても、青丸は1つでよい。
- 今日には黒い輪郭。
- 複数日開催は開催期間中すべての日をイベント日（灰色）として扱うが、青丸は各日行の「行く」を選択した日のみ。
- 日付クリックで表示中の表の該当日へスクロールする。

実地レポート画面：
- 表は「日付｜イベント / 会場｜レポート」の3列。
- 参加条件は表示しない。
- 実地レポートが存在する月をカレンダーに表示し、`visitDates` に含まれる実際の来訪日だけ青丸にする。
- イベント名左の上チェックは「良かった」、下の赤い×は「良くなかった」の目印。
- 良かった行は薄い水色、良くなかった行は薄い赤色にする。
- この評価チェックは実地レポートのカレンダー表示には影響しない。
- レポート本文は `実地レポート.json` を正本とし、評価チェックはブラウザのlocalStorageに保存する。

参加予定・除外状態はイベントID単位でlocalStorageに保存する。
既存イベントIDは移行前と同じ値を維持し、既存チェック状態を引き継げるようにする。

---

## 4.1 Excel移行ルール（2026年 企業展示会簡易レポ.xlsx）

- 「スケジュール」の薄い水色は元々「展示会以外」を示す目印で、懇親会系の強い判定材料だが色だけでは確定しない。
- 明確な例外の「超アミュジョブサミット」は対象外として再候補にしない。
- Microsoft AI Tour / AWS Summit / Google Cloud Next は大型イベントとして展示会側に分類する。
- 展示会内の交流会に固有名がある場合、展示会本体を懇親会側へ重複登録せず、交流会を固有名で登録し「○○内」等を注記する。
- Excelの名前欄は「名前コード」。存在する場合は `名前コード：○○` として参加条件の先頭に保持する。複数名なら全て保持する。
- 名前コードが赤字なら、その名義で過去に審査拒否された意味。`審査ハードル高め（過去に申請拒否あり）` を付ける。2026年Excelでは Dynatrace Innovate Roadshow Tokyo 2026 の伊藤 聡が該当。
- 名前コード以外の文字色には原則意味を持たせない。
- 過去イベントは「参加した」「行っていない」を必ず区別する。結果は年間台帳（`イベント一覧.json` / `展示会一覧.json`）にも保持し、未参加は `未参加イベント.json` にも明示する。
- 実際に参加した複数日イベントは `visitDates` に実際の来訪日だけを入れる。開催期間全日を来訪扱いにしない。
- 既存GitHub側に公式URL・評価・懇親会時間など詳しい情報がある場合は既存情報を優先し、Excelの名前コードや有用メモを補完する。
- 公開リポジトリのため、秘密情報や個別コードは保存しない。公開に適さない具体的な操作手順は転載せず、会場・食事・混雑・粗品・評価などの体験情報を残す。

---

## 5. ChatGPTが守ること

- 作業前に `引き継ぎ.md`、`イベント一覧.json`、`展示会一覧.json`、`実地レポート.json` を読む。
- 必要なファイルだけ変更する。
- 懇親会系の追加・修正は `イベント一覧.json`、展示会系は `展示会一覧.json` を更新する。過去の未参加判定は `未参加イベント.json`、参加後の本文は `実地レポート/` を更新する。
- 表示機能を変えない限り `index.html` 全体を書き換えない。
- 必要のない新しいファイルを作らない。
- `最新版`、`最終版`、`修正版2` などの似たファイルを増やさない。
- 古いファイルを勝手に最新版として使わない。
- 分からないファイルを勝手に削除しない。
- `site/chunk*.txt` は通常運用では変更・削除しない。
- 人が読むファイル名は原則日本語にする。
- `index.html`、`README.md` のようにシステム・慣例上意味のある名前だけ英語のまま残す。
- パスワード、APIキー、ログイン情報などの秘密情報をGitHubに保存しない。

### 通常の更新方法

1. ChatGPTに「台帳を更新して」と依頼される。
2. `引き継ぎ.md` と `イベント一覧.json` を読む。
3. 新規イベント・既存イベントの変更を `イベント一覧.json` へ反映する。
4. 必要に応じてGitHub Pagesの公開URLで反映を確認する。
5. 表示機能の変更が必要な場合だけ `index.html` を変更する。

### 失敗済みなので再提案しないこと

ローカル `E:\無料食事イベント台帳.html` の自動更新は複数方式で失敗済み。

- PowerShell常時監視：常駐監視が不要なので却下。
- ダウンロード後CMDコピー：追加クリックが必要なので却下。
- Chrome拡張＋Native Messaging：ChatGPTのダウンロード経路を取得できず失敗。

したがって、通常ChatGPTからEドライブへ直接自動上書きする案は、状況が変わらない限り再提案しない。

---

## 6. 新しいChatGPTが最初にすること

この案件を続けるよう言われたら、次の順番で確認する。

1. `引き継ぎ.md` を読む。
2. `イベント一覧.json`、`展示会一覧.json`、`実地レポート.json`、`未参加イベント.json` を読む。必要なら `実地レポート/` の該当月も読む。
3. 表示・カレンダー・チェック機能の変更が必要な場合だけ `index.html` を読む。
4. 必要に応じて `README.md` を確認する。
5. GitHub上の現在の状態を基準に作業する。

過去の会話だけを見てイベント一覧や現在状態を推測・復元しない。

---

## 7. 現在の状態

2026-09-18時点：

- GitHubリポジトリ `rensei11/01-event` を使用中。
- GitHub Pagesで公開中。
- 表示プログラムとイベント実データの分離は完了済み。
- 表示プログラムは `index.html`。
- 懇親会の年間台帳は `イベント一覧.json`。
- 展示会の年間台帳は `展示会一覧.json`。
- 参加後の実績本文は `実地レポート/` の月別ファイルで、`実地レポート.json` が読み込み先を管理する。
- 2026年の実地レポート115件と未参加29件は年間台帳へ復元済み。参加済み115件は `attendanceStatus: attended`、未参加29件は `attendanceStatus: not_attended` を保持する。
- 過去に候補だったが行っていないイベントは `未参加イベント.json` に保存する。
- 複数日イベントは内部では1件のまま保持するが、画面では日別行に展開して（1）（2）（3）と表示する。来場予定・実績は `visitDates` で日別管理する。
- 公開画面は「イベント一覧」「来場予定」「実地レポート」の切替式。
- AI向け説明は `引き継ぎ.md`。
- 人間向け案内は `README.md`。
- `site/chunk0.txt` ～ `site/chunk5.txt` は移行前データの復旧用として残している。
- 通常運用では、イベント追加・修正時に `イベント一覧.json` だけを更新する。
