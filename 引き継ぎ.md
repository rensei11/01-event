# 無料食事イベント調査 引き継ぎ

## この案件の目的
東京都内中心で、無料または具体的な無料参加経路があり、17:00以降に懇親会・Networking・Reception・After Party等で食事やアルコール提供が期待できるイベントを探す。

重視条件：
- 原則無料。招待コード等で無料になるものは可。
- 夕方以降の食事・懇親会を最重視。
- 150人以上を優先。小規模は大幅減点。
- 大人数でも軽食のみ・混雑過多は評価を下げる。
- ホテル宴会場、普段入れない会場、料理の量・補充・質を重視。
- ユーザーはビールを飲まない。ワインも基本苦手。ブラックニッカクリアは低評価。
- 杯数課金・チケット制は不可。
- 食品展示会は除外。

## 現在の正本
GitHubリポジトリ：`rensei11/chatgpt-test`

公開URL：
`https://rensei11.github.io/chatgpt-test/`

今後の候補一覧は、会話履歴ではなくGitHub上の現在の台帳を正本とする。

## 現在のGitHub構成（2026-09-16移行済み）
- `index.html`：表示画面・3か月カレンダー・参加予定チェック・除外チェック等のプログラム
- `イベント一覧.json`：イベント名、日付、会場、参加条件、評価、夜の内容、公式URL等の実データ
- `引き継ぎ.md`：この案件のAI向け説明書
- `README.md`：ユーザー向けの簡単な案内板
- `site/chunk0.txt` ～ `site/chunk5.txt`：移行前のHTML本体。復旧用として当面残す。通常更新では触らない

### 最重要ルール
イベントの追加・修正は原則 `イベント一覧.json` だけを更新する。
表示やカレンダー・チェック機能を変更するときだけ `index.html` を更新する。

旧 `site/chunk*.txt` は新しい公開ページでは読み込まない。移行直後の復旧用なので、安定確認前に削除しない。

## UI仕様
イベント名の左に参加予定用チェック、会場名の左に除外用チェックを置く。

参加予定チェック：
- 青いチェックマーク。
- チェック時、そのイベント行全体を薄い水色にする。
- カレンダー上でもその日を青丸にする。

除外チェック：
- 通常は空の四角。
- クリックすると赤背景＋白い「×」。
- 説明文字は表示しない。
- 除外した行は薄い灰色表示。
- 参加予定チェックとは同時選択不可。

カレンダー：
- 当月＋次2か月。
- イベントのある日は通常「灰色」の丸。
- 参加予定イベントがある日は「青色」の丸。
- 今日には黒い輪郭。
- 日付クリックでその日のイベントへスクロール。

参加予定・除外状態はイベントID単位でlocalStorageに保存する。
既存イベントIDは移行前と同じ値を維持し、既存チェック状態を引き継げるようにする。

## 更新方法
通常ChatGPTからGitHubコネクタを使って直接更新する。

通常運用：
1. ChatGPTに「台帳を更新して」と依頼
2. ChatGPTが `イベント一覧.json` を読む
3. 新規イベント・既存イベントの変更をJSONへ反映
4. GitHub Pagesの公開URLで反映確認

表示機能を変えない限り `index.html` 全体を書き換えない。

## 失敗済みなので再提案しないこと
ローカル `E:\無料食事イベント台帳.html` の自動更新は複数方式で失敗。
- PowerShell常時監視：常駐監視が不要なので却下。
- ダウンロード後CMDコピー：追加クリックが必要なので却下。
- Chrome拡張＋Native Messaging：ChatGPTのダウンロード経路を取得できず失敗。

したがって、通常ChatGPTからEドライブへ直接自動上書きする案は再提案しない。

## ファイル名ルール
人が読むファイル名は原則日本語にする。
例：`引き継ぎ.md`、`イベント一覧.json`。

`index.html`、`README.md` のようにシステム・慣例上意味のある名前だけ英語のまま残す。

## 次のチャットで最初にやること
ユーザーが「GitHubの引き継ぎ.mdと現在の台帳を読んで続けて」と言ったら、
1. `引き継ぎ.md` を読む
2. `イベント一覧.json` を読む
3. 必要な場合だけ `index.html` を読む
4. その内容を基準に調査・更新する

イベント一覧を会話履歴から推測して復元しない。
