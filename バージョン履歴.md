# 無料食事イベント台帳 バージョン履歴

## 現在

- **本番版：v0.32**
- 対象：`index.html`
- **確認用UI：UI試作12**
- 対象：`確認用.html`
- 確認用UIは **本番基準 v0.32** から分岐しているが、本番未反映の機能・見た目変更を含む。

本番版とUI試作番号は別物です。
確認用を何回修正しても、本番へ採用するまでは本番版を上げません。

## 本番版の付け方

- **+0.01**：小さな修正、軽いUI改善、不具合修正
- **+0.02〜0.05**：まとまった機能追加・改善
- **+0.10**：使い方や構造が大きく変わる追加
- **+1.00**：一つの世代として明確に完成した節目だけ
- イベント情報の追加、URL・時刻・参加結果などの**データ更新だけでは本番版を上げない**
- 1コミット=1バージョンにはしない。同じ依頼・同じ目的の複数コミットは1つの変更群として扱う

## 復元した本番版

### v0.01 — 2026-09-16
- GitHub Pagesで台帳を表示する初期Web版。
- 既存台帳をブラウザで確認するための入口を作成。

### v0.10 — 2026-09-16
- 表示プログラムとイベント実データを分離。
- `イベント一覧.json` を正本として読み込む構造へ移行。
- 巨大HTML全体を書き換えず、イベントデータを更新できる土台を作った大きな構造変更。

### v0.15 — 2026-09-18
- 懇親会だけでなく展示会・大型イベントを同じ台帳画面で扱う構成へ拡張。
- イベント一覧、来場予定、展示会系の表示を統合。

### v0.20 — 2026-09-18
- Excelから2026年の予定・参加・未参加履歴を移行。
- 実地レポート、参加済み履歴、未参加履歴をGitHub上の年間データとして整理。
- 複数日イベントの実参加日を区別する構造を導入。

### v0.30 — 2026-09-18
- 終了処理を追加。
- 参加／未参加、実参加日、評価、実地レポート等を処理できるようにした。
- Cloudflare Worker経由でGitHubへ確定保存する経路を追加。
- 実地レポートを台帳へ統合。

### v0.31 — 2026-09-18〜09-19
- 複数日イベント、灰色除外、月表示、参加日表示等を実運用向けに調整。
- 終了処理前後の編集・参加済み表示を整理。

### v0.32 — 2026-09-19（現在の本番）
- 来場予定から既参加日を除外。
- 本番画面の参加予定・参加履歴まわりを調整。
- 2026-09-19 03:04頃を最後に、本番 `index.html` は見た目再設計から分離。
- 以後の大規模な見た目変更は `確認用.html` で試験している。

## 確認用UIの系統

`確認用.html` は2026-09-19に「本番を変えずに見やすさを試すページ」として分離したものです。
ここでの番号は**本番版ではありません**。

### UI試作0
旧表記：`2026-09-26-v0`

- 9/26のカレンダー再設計直前。
- 3か月カレンダー。
- 次回参加予定イベント欄なし。
- 退避：`archive/確認用_2026-09-26_v0.html`

### UI試作1
旧表記：`2026-09-26-v1`

- カレンダーを3か月から2か月へ変更。
- 旧3枚目の領域へ「次回参加予定イベント」を追加。
- 「来場予定」を「参加予定」へ変更。
- カレンダー左右スライドを追加。
- 退避：`archive/確認用_2026-09-26_v1.html`

### UI試作2
旧表記：`2026-09-26-v2`

- 次回参加予定欄をカレンダースライドから分離して固定。
- 月送りボタンをカレンダー内へ移動。
- マウスホイールで月移動。
- 退避：`archive/確認用_2026-09-26_v2.html`

### UI試作3

- 次回参加予定を1件ずつ表示し、左右ボタンで切替。
- 対象を明日以降かつ実際の当月・来月へ限定。
- 展示会／懇親会バッジを追加。
- カレンダー月送りボタンの中央配置・サイズを改善。
- 東京ビッグサイト同日イベントの並びを東→西→南・ホール番号優先へ整理。
- 確認ページ上では **「本番基準 v0.32 / UI試作3」** と表示する。

### UI試作4

- ビッグサイト会場名を `東京ビッグサイト …` に統一。
- 省略形の `東1-3ホール`、`西1ホール`、`南1-4ホール` 等も正規化。
- 旧表記の `ビックサイト …` も `東京ビッグサイト …` に統一。
- イベント名のハイパーリンクへ下線を追加。
- 未参加イベントはイベント名とリンク文字も灰色表示。
- UI試作3を `archive/確認用_2026-09-26_v3.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作4」** と表示する。

### UI試作5

- 次回参加予定欄を「1イベントずつ」ではなく「1参加予定日ずつ」の切替へ修正。
- 同じ日付の参加予定イベントは、その日付のカード内へすべて同時表示する。
- 左右ボタンは前後のイベントではなく、前後の参加予定日へ移動する。
- 当月・来月という表示対象範囲は維持。
- UI試作4を `archive/確認用_2026-09-26_v4.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作5」** と表示する。

### UI試作6

- 各イベント右上に「イベント編集」を追加。
- 終了済みイベントは「イベント編集」の下に「終了処理」または「記録修正」を縦並びで表示。
- 参加名義を右上から会場の下へ移動。
- イベント編集では、イベント名、開始日、終了日、会場、参加名義、URL、参加条件、事前評価、時間、内容・根拠を編集可能。
- イベント編集は実地レポートを変更せず、年間台帳のイベント本体だけを editOnly でGitHubへ保存する。
- Workerソースの editOnly に日付更新を追加。未参加イベントは未参加台帳も同期する。
- 旧「編集開始」は「一覧直接編集（旧）」と明示し、通常修正はイベント編集を使う案内へ変更。
- UI試作5を `archive/確認用_2026-09-26_v5.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作6」** と表示する。

### UI試作7

- 参加名義を会場と同じ行・同じ文字サイズへ変更し、行の高さを増やさない表示へ変更。
- 会場と参加名義の間隔は約半角1文字分。
- 名義未入力でも「参加名義：」は常に表示する。
- 「行かない」をONにした時、参加名義がある場合はイベント情報から参加名義を自動削除し、editOnlyでGitHubへ保存する。
- カレンダー右側の「次回参加予定イベント」は「行く」がONのイベントだけを対象にし、「興味あり」は除外する。
- 上部の「参加予定」一覧では従来どおり「興味あり」も対象のまま。
- UI試作6を `archive/確認用_2026-09-26_v6.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作7」** と表示する。

### UI試作8

- 参加名義を会場と同じ行に置く変更を取り消し、会場の下の別行へ戻した。
- 参加名義は会場と同じ14px。左を約半角1文字分だけインデント。
- 「参加名義：」は太字。通常は名義未入力でもラベルを表示する。
- 「行かない」のイベントは参加名義行そのものを非表示にし、「参加名義：」の文字も残さない。
- カレンダー上部の独立した「カレンダー」見出しと右側の凡例説明を削除。
- 「カレンダー」を「表の表示開始月」の左へ移動し17pxへ拡大。
- 「表：2026年9月以降」等の状態文言と「月ボタンで表とカレンダーを同じ月へ移動」の説明を削除。
- 次回参加予定イベント欄のイベント名を12px→14px、参加名義・場所を10px→11pxへ拡大。
- 次回参加予定イベント欄の展示会／懇親会バッジ、参加名義、場所を縦中央で揃えた。
- UI試作7を `archive/確認用_2026-09-26_v7.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作8」** と表示する。

### UI試作9

- カレンダー操作行の右側に空いていた余白を使い、「次回参加予定イベント」枠を上へ移動。
- 左側を「カレンダー操作行＋2か月カレンダー」の1列としてまとめ、右側の次回予定枠をその列全体と並べる構成へ変更。
- 次回参加予定が1件・2件でも、右枠の下端は左側カレンダーの下端まで伸ばす。
- 件数によって右枠の高さが変形しない固定的な見た目にした。
- UI試作8を `archive/確認用_2026-09-26_v8.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作9」** と表示する。

### UI試作10

- イベント編集と「行かない」の参加名義削除から、未更新Cloudflare Workerへの editOnly 保存呼び出しを停止。
- GitHub上の最新Workerソースは editOnly 対応済みだが、実稼働Workerは未更新で「参加／未参加を選んでください」を返す状態のため、UI側で切り離した。
- イベント編集フォームは入力のたびに localStorage へ自動保存し、保存失敗や画面再読み込みで文章を失わないようにした。
- 「下書きを反映」でイベント本体へ反映し、既存の構造化編集下書きへ保存する。
- 「行かない」で参加名義を消す処理もWorkerへ送らず、ローカル構造化下書きへ自動保存する。
- イベント編集から古いWorkerを呼ぶ未使用関数を削除し、保存関数名もブラウザ下書き保存であることが分かる名称へ変更。
- UI試作9を `archive/確認用_2026-09-26_v9.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作10」** と表示する。

### UI試作11

- 「行かないを非表示」チェックを追加。ON時は「行かない」チェック済みイベントを一覧・共通表示対象から除外する。
- 非表示設定はUI状態としてlocalStorageへ保存する。
- カレンダー左上「カレンダー」を17px→19px。
- 「表の表示開始月」、年選択を12px→14px。
- 「全期間」「1月～12月」ボタンを11px→13px。
- 各月見出し「2026年9月」等を13px→16px。
- 曜日を13pxにし、日付12pxより1px大きくした。日曜は赤、土曜は青。
- 「次回参加予定イベント」と日付を13px→15px、双方太字。
- 次回参加予定のイベント名を14px・font-weight 900で明示固定。
- 次回参加予定の参加名義・場所は黒に近い90%濃度へ変更。
- 「参加名義：」「場所：」のみ太字、値部分は通常ウェイト。
- UI試作10を `archive/確認用_2026-09-26_v10.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作11」** と表示する。

### UI試作12 — 現在

- 下の一覧の月見出しを15px→18pxへ拡大。
- 一覧の月見出し・週見出しを全角1文字分だけ右へインデント。
- 次回参加予定イベント欄の参加名義・場所を11px→12px。
- 「参加名義：」「場所：」はラベル＋コロンのみ太字、右側の値は通常ウェイトを維持。
- 各イベント右上の操作ボタン幅を縮小。操作列の最小幅を82px→74px。
- 「イベント編集」の左右余白を7px→4px、「終了処理／記録修正」の左右余白を7px→5pxへ縮小。
- UI試作11を `archive/確認用_2026-09-26_v11.html` に退避。
- 確認ページ上では **「本番基準 v0.32 / UI試作12」** と表示する。

## 本番反映するとき

ユーザーが確認用UIを「本番へ反映」と明示した時だけ、次を行う。

1. `確認用.html` で採用する変更と、試験中で残す変更を確定する。
2. 本番 `index.html` へ必要な変更だけ反映する。
3. 変更量に応じて本番版を上げる。
4. `バージョン履歴.md`、`現在の仕様.md`、必要な画面上の版表示を同じ本番版へ更新する。
5. 確認用UIの試作番号は次の試験サイクルとして別管理する。

現在のUI試作群がそのまま全採用される場合は比較的大きな更新になる可能性があるが、**本番採用内容が確定する前に次の本番版番号を先に決めない**。

## ロールバック

既存の退避ファイルは名前を変更せず保持する。

- `archive/確認用_2026-09-26_v11.html` → UI試作11
- `archive/確認用_2026-09-26_v10.html` → UI試作10
- `archive/確認用_2026-09-26_v9.html` → UI試作9
- `archive/確認用_2026-09-26_v8.html` → UI試作8
- `archive/確認用_2026-09-26_v7.html` → UI試作7
- `archive/確認用_2026-09-26_v6.html` → UI試作6
- `archive/確認用_2026-09-26_v5.html` → UI試作5
- `archive/確認用_2026-09-26_v4.html` → UI試作4
- `archive/確認用_2026-09-26_v3.html` → UI試作3
- `archive/確認用_2026-09-26_v2.html` → UI試作2
- `archive/確認用_2026-09-26_v1.html` → UI試作1
- `archive/確認用_2026-09-26_v0.html` → UI試作0

過去ファイル名の `v0/v1/v2` は歴史的名称であり、本番版番号とは無関係。
